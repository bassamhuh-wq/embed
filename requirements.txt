# المكتبة الأساسية للتعامل مع بوت تيليجرام
python-telegram-bot==20.3

# مكتبة لإدارة متغيرات البيئة (مثل التوكن) بشكل آمن
python-dotenv

# مكتبة لمراقبة موارد النظام (المعالج، الرامات) - مفيدة جدًا للإدارة
psutil

# مكتبة للتعامل مع الصور (لو احتجت تعديل الشعار أو معالجته محليًا)
Pillow

# مكتبة لإجراء طلبات HTTP غير متزامنة (للتوسع في المستقبل)
aiohttp